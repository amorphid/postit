<h1>Post</h1>

<%= link_to "Edit", edit_post_path(@post) %>

<p>
  Title:
  <%= @post.title %>
</p>


<p>
  URL:
  <%= @post.url %>
</p>

<%= render "comments/form" %>

<ul>
  <% @comments.each do |comment| %>
    <li>
      <%= comment.body %>
    </li>
  <% end %>
</ul>
